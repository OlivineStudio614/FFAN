// Real widget tests live under test/features/. This placeholder keeps the
// default test target compiling until the app is wired together.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(true, isTrue);
  });
}
